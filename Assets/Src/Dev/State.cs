// ITS GONNA BE REDONE, SO DONT PAY ATTENTION TO THIS CODE
/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class State
{
    private Vector3 pos { get; set; }
    private Vector3 lookAt { get; set; }
    private float speed { get; set; }

    public State()
    {
        pos = Vector3.zero;
        lookAt = Vector3.zero;
        speed = 0f;
    }

    public State(Vector3 pos, Vector3 lookAt, float speed)
    {
        this.pos = pos;
        this.lookAt = lookAt;
        this.speed = speed;
    }

    public bool EqualsZero()
    {
        if (this.pos == Vector3.zero && this.lookAt == Vector3.zero && this.speed == 0f)
            return true;
        else 
            return false;
    }
}
*/